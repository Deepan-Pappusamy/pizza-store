import "../styles/initialLoader.css";

const InitialLoader = () => {
  return (
    <div className="initial-loader-screen">
      <h4 className="initial-loader-title">Dee Pizza Store</h4>
      <p>Fetching data please wait...</p>
    </div>
  );
};

export default InitialLoader;
